<main class="container">
    <div class="header">
      <h1>Quotes</h1>
      <%= link_to "New Quote",
                  new_quote_path,
                  data: { turbo_frame: dom_id(Quote.new)},
                  class: "btn btn--primary" %>
    </div>

  <%= turbo_frame_tag Quote.new %>

  <%= render @quotes %>
</main>