<main class="container">
  <h1>Quote editor</h1>
  <p>A lightning-speed quote web app crafted with Hotwire & Stimulus</p>

  <% if user_signed_in? %>
    <%= link_to "View quotes", quotes_path, class: "btn btn--dark" %>
  <% else %>
    <%= link_to "Sign up", new_user_registration_path, class: "btn btn--dark" %>
  <% end %>
</main>